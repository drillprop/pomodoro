import styled from 'styled-components';
import { ResetandRetryButtons } from '../Control.styles';

export const StyledRetryButton = styled(ResetandRetryButtons)`
  border-radius: 0 21px 21px 0;
  grid-column: 2;
  padding-left: 1em;
`;
