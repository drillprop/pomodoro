import React from 'react';
import { LoadingWrapper, Spinner } from './Loading.styles';

const Loading = () => {
  return (
    <LoadingWrapper>
      <Spinner />
    </LoadingWrapper>
  );
};

export default Loading;
