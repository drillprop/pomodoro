import styled from 'styled-components';

export const StyledSvg = styled.svg`
  display: block;
  fill: #333;
  position: absolute;
  top: 130px;
  left: 50%;
  transform: translate(-50%, 0);
  width: 500px;
  z-index: -2;
`;
